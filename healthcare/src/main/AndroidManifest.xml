<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ir.madjeed.healthcare" >


    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" >
        <activity
                android:name=".gui.menu.StarterActivity"
                android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"></action>
                <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>
        </activity>

        <activity android:name=".gui.EditInfoActivity"/>
        <activity android:name=".gui.ConsultantActivity"/>
        <activity android:name=".gui.ConsultantDetailActivity"/>
        <activity android:name=".gui.AddConsultantActivity"/>
        <activity android:name=".gui.ProfileActivity"/>

        <activity android:name=".gui.MenuActivity"/>
        <activity android:name=".gui.menu.PatientMenuActivity"/>

        <activity android:name=".gui.ItemDetailActivity"/>
        <activity android:name=".gui.MyListActivity"/>


        <activity android:name=".gui.authentication.RegisterActivity"/>
        <activity android:name=".gui.authentication.LoginActivity"/>

    </application>

</manifest>
